BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Supply] ADD [isDeliveredToInventory] BIT NOT NULL CONSTRAINT [Supply_isDeliveredToInventory_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
