BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[IngredientItem] ADD [unit] NVARCHAR(1000) NOT NULL CONSTRAINT [IngredientItem_unit_df] DEFAULT 'GAM';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
