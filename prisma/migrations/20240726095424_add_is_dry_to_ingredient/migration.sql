BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[IngredientItem] ADD [isDry] BIT NOT NULL CONSTRAINT [IngredientItem_isDry_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
